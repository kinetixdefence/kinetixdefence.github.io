# Optional — only needed if you want to preview the site locally with
# `bundle exec jekyll serve`. GitHub Pages itself does NOT need this file;
# it builds Jekyll sites automatically from _config.yml with no build step
# on your part. See README.md.

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"
